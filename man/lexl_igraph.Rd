% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lexl_igraph.R
\name{lexl_igraph}
\alias{lexl_igraph}
\alias{plot.lexl}
\alias{lexl_edges}
\alias{lexl_vertices}
\title{Plot the parse tree of a formula}
\usage{
\method{plot}{lexl}(x, ...)

lexl_edges(x)

lexl_vertices(x)

lexl_igraph(x)
}
\arguments{
\item{x}{parse tree from \code{lexl()}}

\item{...}{ignored, for consistency with the base \code{plot()} function}
}
\description{
Requires the \code{igraph} and \code{ggraph} packages, which you
can install with \code{install.packages(c("igraph", "ggraph"))}.  Draws a
simple tree plot of the parse tree, showing parent-child relationships,
tokens and types.

The utility functions \code{lexl_edges()}, \code{lexl_vertices()}, and \code{lexl_igraph()}
compute each stage in plotting the final graph.
}
\examples{
x <- lex_xl("MIN(3,MAX(2,A1))")

lexl_edges(x)
lexl_vertices(x)

if (interactive() && require(igraph, quietly = TRUE)) {
  lexl_igraph(x)
}

if (interactive()
    && require(igraph, quietly = TRUE)
    && require(ggraph, quietly = TRUE)) {
  plot(x)
}
}
\seealso{
\code{\link[lexl:lex_xl]{lexl::lex_xl()}}, \code{\link[lexl:demo_lexl]{lexl::demo_lexl()}}
}
